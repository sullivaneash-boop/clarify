VITE_SUPABASE_URL=https://bjtfnlvceaopvgoovflm.supabase.co
# Browser-safe key only: use the Supabase anon/public or publishable key.
# Never use the service_role/secret key in a VITE_ variable.
VITE_SUPABASE_ANON_KEY=

# Server-side only. Do not expose in browser.
# Set this in Supabase Edge Function secrets, not as VITE_GEMINI_API_KEY.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
LLM_PROVIDER=stub
