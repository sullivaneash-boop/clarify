VITE_SUPABASE_URL=https://bjtfnlvceaopvgoovflm.supabase.co
# Browser-safe key only: use the legacy Supabase anon JWT key for this Edge Function.
# Never use the service_role/secret key in a VITE_ variable.
VITE_SUPABASE_ANON_KEY=

# Server-side only. Do not expose in browser.
# Set this in Supabase Edge Function secrets, not as VITE_DEEPSEEK_API_KEY.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash
LLM_PROVIDER=stub
